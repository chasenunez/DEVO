Metadata-Version: 2.4
Name: devo
Version: 0.1.0
Summary: DEVO — CSV -> iCSV enrichment + Frictionless validation tooling
License: MIT
Project-URL: Source, https://example.org/devo
Requires-Python: >=3.9
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: frictionless>=4.0.0
Requires-Dist: flask>=2.0.0
Dynamic: license-file

# DEVO

DEVO converts CSV files into self-documented iCSV files (iCSV 1.0 UTF-8) and validates data with the Frictionless framework.

## Quickstart

Install (locally):

```bash
pip install -e .
```

Create an iCSV and schema from `examples/sample.csv`:

```bash
python -m devo.cli enrich examples/sample.csv --out DEVO_output
```

Validate an iCSV (produces markdown report):

```bash
python -m devo.cli validate DEVO_output/sample.icsv --out DEVO_output
```

Run the tiny web UI (simple Flask app):

```bash
export FLASK_APP=devo.webui
flask run
```

Open `http://127.0.0.1:5000` to upload a CSV and run enrich+validate.
